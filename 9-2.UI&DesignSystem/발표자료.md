# 9회-2: UI & 디자인 시스템

## 학습 범위

| 섹션 | 확인할 내용 |
|---|---|
| Frontend 빌드 체계 | React 기반 UI, Node/Yarn 개발 환경, UI 개발 서버 |
| 커넥터 UI 통합 | JSON Schema, quicktype, `source_type` 연결 지점 |
| Theme & 브랜딩 | Primary Color와 전역 UI 반영 |
| Persona | Persona 정의, 사용자 매핑, 선택 Persona |
| Landing Page | Pluggable Panel, 위젯 추가·제거·재배열 |

---

## 1. Frontend 빌드 체계

**코드 위치**

`openmetadata-ui/src/main/resources/ui`

- React 기반 UI 프로젝트
- Node와 Yarn으로 의존성·개발 서버 관리
- Vite 개발 서버: `package.json`의 `start`

```bash
make yarn_install_cache
make yarn_start_dev_ui
```

![Frontend build flow](발표자료-assets/frontend-build-flow.svg)

---

## 2. JSON Schema와 커넥터 UI

**UI 페이지**

`Settings > Services > Add New Service`

1. JSON Schema에서 연결 정보와 `source_type`을 정의한다.
2. `quicktype`이 UI용 TypeScript 타입을 생성한다.
3. 서비스 설정 UI는 생성 타입과 연결 규칙을 사용한다.

```bash
# openmetadata-ui/.../json2ts.sh
quicktype -s schema "$tmp_schema_file" \
  -o "$output_file" --just-types
```

![Schema to type flow](발표자료-assets/schema-type-flow.svg)

**코드 위치**

- Schema: `openmetadata-spec/src/main/resources/json/schema/`
- 생성 스크립트: `openmetadata-ui/src/main/resources/ui/json2ts.sh`
- 생성 타입: `openmetadata-ui/src/main/resources/ui/src/generated/`

---

## 3. Theme & 브랜딩

**UI 페이지**

`Settings > Preferences > Theme`

| 설정 | UI 반영 범위 |
|---|---|
| `primaryColor` | 기본 강조색, 버튼, 내비게이션 |
| `hoverColor` | hover 상태 |
| `selectedColor` | 선택 상태 |
| 로고·모노그램·favicon | 브랜드 식별 요소 |

```typescript
const palette = generatePalette(primaryColor);
const hoverColor = savedHoverColor || palette[2];
const selectedColor = savedSelectedColor || palette[8];
```

![Theme propagation flow](발표자료-assets/theme-flow.svg)

---

## 4. Persona 기반 개인화

**UI 페이지**

`Settings > Personas`

- Persona: 역할별 UI 구성을 위한 엔티티
- User: 기본 Persona를 지정할 수 있음
- Selected Persona: 현재 화면이 참조하는 Persona

```typescript
const pageFQN =
  `${EntityType.PERSONA}.${selectedPersona?.fullyQualifiedName}`;
const doc = await getDocumentByFQN(pageFQN);
```

![Persona selection flow](발표자료-assets/persona-flow.svg)

**코드 위치**

- Schema: `openmetadata-spec/.../entity/teams/persona.json`
- UI 조회: `src/hooks/useCustomPages.ts`

---

## 5. 커스터마이즈 가능한 Landing Page

**설정 UI 페이지**

`Settings > Personas > <Persona> > Customize UI > Landing Page`

**렌더링 UI 페이지**

`Home / My Data`

- Persona별 Landing Page에 위젯을 추가·제거·재배열한다.
- 저장된 레이아웃은 선택 Persona의 페이지 구성으로 읽힌다.
- 대표 위젯: Activity Feed, KPI, Announcements, Tasks, Recently Viewed

```typescript
const customizedPage = doc.data?.pages?.find(
  (page) => page?.pageType === pageType
);
```

![Landing page layout flow](발표자료-assets/landing-layout-flow.svg)

---

## 데모 순서 (10분)

1. `make yarn_install_cache` → `make yarn_start_dev_ui`로 UI 개발 서버를 실행한다.
2. `Settings > Preferences > Theme`에서 Primary Color 변경 결과를 확인한다.
3. Persona를 만든 뒤 Landing Page에서 위젯을 추가·제거·재배열한다.

---

## 토론 질문

1. JSON Schema 변경 뒤 타입 생성과 UI 연결은 어느 지점에서 확인해야 하는가?
2. Persona와 Pluggable Panel 설정은 어떤 UI 페이지에서 관리하는가?
3. 디자인 시스템 관련 소스는 어느 경로부터 추적하는가?

---

## 필수 읽기

- `[docs]/developers/contribute/build-code-and-run-tests/openmetadata-ui.md`
- `[docs]/developers/contribute/build-code-and-run-tests/generate-types-from-json-schema.md`
- `[docs]/how-to-guides/guide-for-data-users/theme.md`
- `[docs]/how-to-guides/admin-guide/persona-landing-page-customization/defining-persona.md`
- `[docs]/how-to-guides/admin-guide/persona-landing-page-customization/customizing-landing-page.md`
